# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-single-view-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Protobuf-C++', :git => 'https://github.com/protocolbuffers/protobuf.git', :tag => 'v3.11.1'

  # Pods for ios-single-view-app

  target 'ios-single-view-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
